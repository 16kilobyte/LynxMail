import Account from './account.model';
import Token from './token.model';
import Email from './email.model';
import EmailBody from './email.body.model';
import EmailAddress from './email.address.model';

const schemas = [
  Account,
  Token,
  Email,
  EmailAddress,
  EmailBody
];

export default schemas;